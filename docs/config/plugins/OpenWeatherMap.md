## OpenWeatherMap

Uses the free Current Weather Data endpoint — current conditions only, no forecast (OpenWeatherMap's forecast-capable One Call API requires a separate paid subscription). Requires a free API key from https://openweathermap.org/api.

```ini
; CONFIG_DIR/plugins/OpenWeatherMap.ini
[plugin]
enabled = True
apikey = {API Key}
```
