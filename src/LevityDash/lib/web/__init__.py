"""LevityWeb - web service protocol and scene extraction.

A headless Qt frontend that flattens the live dashboard into JSON-safe layout
items and streams them to browser clients, which render with their own GPU
engine. See service.py (the process) and layout.py (the flattening).
"""
